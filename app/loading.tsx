import CommonLoading from '@/components/CommonLoading';

const AppLoading = () => {
  // You can add any UI inside Loading, including a Skeleton.
  return <CommonLoading isLoading title='Loading...' />;
};

export default AppLoading;
